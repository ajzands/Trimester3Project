var random = (Math.random());
var multiply = random * 100;
var round = Math.round(multiply) ;
var number = round;

for(g = 0; g < 8; g++)

var guess = prompt("I thought of a Number 1-100, Guess my Number, You have 9 Chances!")

if(guess < number){

	alert("higher." + c);
}

else if(guess > number)
{
	alert("lower." + c);
}
else
{
	alert("You guessed it!" + number);
}
}
